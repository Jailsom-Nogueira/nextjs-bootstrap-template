---
description: Vitest + RTL unit tests, Playwright e2e — determinism, no skipped tests, MSW where needed
globs:
  - src/**/*.test.ts
  - src/**/*.test.tsx
  - e2e/**/*.spec.ts
  - vitest.config.ts
  - vitest.setup.ts
  - playwright.config.ts
alwaysApply: false
---

Auto-attaching Cursor pointer. The canonical rules and procedure for tests live at:

- `.agents/rules/qa-loop.md` — the fix-until-green QA loop and anti-patterns.
- `.agents/workflows/qa-loop.md` — the runbook (gates, iteration cap, escalation).
- `.agents/workflows/self-review.md` — the pre-completion checklist.

Read those before editing files matched by the globs above. Do not duplicate their content here. If this stub disagrees with AGENTS.md or with the rule files above, those win and this stub should be patched.
