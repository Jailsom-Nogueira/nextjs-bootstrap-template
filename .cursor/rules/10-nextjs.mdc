---
description: Next.js 16 App Router patterns — Server Components first, Server Actions, route handlers, middleware/proxy, lazy loading
globs:
  - src/app/**
  - src/components/**
  - src/proxy.ts
  - next.config.ts
alwaysApply: false
---

Auto-attaching Cursor pointer. The canonical rules for this surface live at:

- `.agents/rules/performance.md` — Server Components, Web Vitals, INP, bundle budget.
- `.agents/rules/lazy-loading.md` — `next/dynamic`, `Suspense`, route-level `loading.tsx`.
- `.agents/rules/clean-code.md` — function size, naming, immutability.
- `.agents/rules/file-organization.md` — naming, layout, path alias, `types.ts`.
- `.docs/nextjs-conventions.md` — concrete Next 16 / React 19 do/don't table.

Read those before editing files matched by the globs above. Do not duplicate their content here. If this stub disagrees with AGENTS.md or with the rule files above, those win and this stub should be patched.
