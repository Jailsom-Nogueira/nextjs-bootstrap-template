---
description: Supabase client split, RLS-first, service-role gating, migrations
globs:
  - src/supabase/**
  - src/app/api/**
  - src/lib/auth/**
  - supabase/migrations/**
alwaysApply: false
---

Auto-attaching Cursor pointer. The canonical rules for this surface live at:

- `.agents/rules/supabase.md` — 4-client split, RLS, types, migrations.
- `.agents/rules/security.md` — env, secrets, CSP, server/client boundaries.
- `.agents/rules/admin.md` — admin routes, role gates, `isAdmin()`, service role.

Read those before editing files matched by the globs above. Do not duplicate their content here. If this stub disagrees with AGENTS.md or with the rule files above, those win and this stub should be patched.
