# Claude Code Project Memory

@./AGENTS.md
@./.agents/rules/qa-loop.md

For project-level slash commands see `.claude/commands/`.
Before completing any task: `npm run qa` until exit 0. UI/browser-facing work also requires `npm run qa:visual`; PR/release requires `npm run qa:strict`.
