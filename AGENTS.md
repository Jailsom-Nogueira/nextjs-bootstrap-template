# AGENTS.md

Hub for AI coding agents (Claude Code, Cursor, Codex, Gemini, Aider, Cline, etc.) working in this repo.

Read this file first. Treat it as a routing index: it tells you what to load next. Do not turn it into long-form docs or duplicate rule-file content here.

Do not assume one fixed persona. Derive the working stance from the classified task and loaded rules: engineer for implementation, QA/reviewer for validation, UI/product designer for visual work, tech lead/architect for specs and plans, technical writer for docs, security reviewer for auth/API/risk work, and the relevant specialist stance for any other surface.

Exact stack versions live in `package.json`. Human quickstart and stack overview live in `README.md`. If a concept is unfamiliar, read `CONCEPTS.md`; teaching belongs there, not here.

## Operating protocol

1. Start here.
2. Classify the task from evidence, not only from the prompt: mentioned paths, `git status`, active `.plans/*`, current diff, recent commits, reported errors, UI symptoms, requested output, and artifact type.
3. Load the smallest safe union of rule files from the task router below. If a task spans multiple surfaces, load all relevant rules.
4. Work in the correct artifact layer. For docs/specs/plans/artifacts, read `.agents/references/artifact-layers.md` before creating, moving, or editing durable artifacts.
5. Before completion, run the required QA for any repo-changing task and run the self-review workflow.

Ask one focused question only when ambiguity changes artifact type, side effects, or user-visible behavior. Otherwise choose the safest default and continue.

## What belongs where

| Layer                 | Purpose                                                                                  |
| --------------------- | ---------------------------------------------------------------------------------------- |
| `AGENTS.md`           | Terse routing hub: protocol, task router, invariant pointers.                            |
| `.agents/rules/`      | Mandatory domain rules that agents must obey when that surface is touched.               |
| `.agents/workflows/`  | Repeatable procedures such as QA, self-review, and multi-agent handoff.                  |
| `.agents/references/` | Lookup material: artifact taxonomy, path maps, component catalogs, event catalogs.       |
| `CONCEPTS.md`         | Long-form teaching: what / why / how / common mistakes.                                  |
| `README.md`           | Human quickstart, stack overview, project map, scripts.                                  |
| `.docs/`              | Durable product/technical docs, specs, architecture, decisions, runbooks.                |
| `.plans/`             | Active implementation plans. Completed or superseded plans move to `.plans/archived/`.   |
| Tool-specific files   | Thin adapters only. They point back to AGENTS.md and must not duplicate canonical rules. |

If a new fact could fit in more than one layer, decide the layer explicitly and prefer the dedicated file over AGENTS.md.

## Non-negotiable invariants

- Infer task type from evidence before editing.
- Load the rule files for every affected surface; project rules override generic agent habits.
- For any repo-changing task, `npm run qa` exit 0 is the baseline definition of done.
- UI/browser-facing changes also require `npm run qa:visual` exit 0. Do not say the user must verify visually when browser/Playwright tooling is available.
- PR/release readiness requires `npm run qa:strict`.
- Do not bypass gates with `any`, `eslint-disable`, `.skip()`, unreviewed `@ts-expect-error`, commented-out code, or `git commit --no-verify` for application commits. Fix the root cause or escalate per `.agents/rules/qa-loop.md`.
- `CHANGELOG.md` is generated by `npm run push`; do not edit it manually.
- Per-agent files (`CLAUDE.md`, `GEMINI.md`, `.cursor/rules/*`, `.clinerules/*`, `CONVENTIONS.md`, etc.) are adapters. If they disagree with this file, AGENTS.md wins and the adapter should be patched.

## Task router

Paths are relative to repo root. Load files in the listed order.

| Task surface                          | Required reads                                                                                                                                   | Required verification                                                                                    |
| ------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------- |
| Read-only question / audit            | Relevant rules or references only; cite inspected files.                                                                                         | No QA command unless files changed.                                                                      |
| Any repo-changing task                | `.agents/rules/qa-loop.md`; before final response: `.agents/workflows/self-review.md`.                                                           | `npm run qa` until exit 0, or blocker plan per QA rule.                                                  |
| UI / styling / components             | `.agents/rules/styling.md`, `responsiveness.md`, `accessibility.md`, `performance.md`, `lazy-loading.md`; add `i18n.md` for user-facing strings. | `npm run qa` and `npm run qa:visual`.                                                                    |
| New page / route                      | `.agents/rules/file-organization.md`, `performance.md`, `lazy-loading.md`, `accessibility.md`, `responsiveness.md`, `i18n.md`.                   | `npm run qa` and `npm run qa:visual`.                                                                    |
| Forms / inputs                        | `.agents/rules/forms.md`, `accessibility.md`, `i18n.md`, `clean-code.md`.                                                                        | `npm run qa`; add visual QA when browser-facing.                                                         |
| Data fetching / Supabase / migrations | `.agents/rules/supabase.md`, `security.md`, `error-handling.md`, `performance.md`.                                                               | `npm run qa`; use DB/type generation commands only when the task needs them.                             |
| Auth / role gates / admin             | `.agents/rules/security.md`, `admin.md`, `supabase.md`, `accessibility.md` for UI.                                                               | `npm run qa`; add visual QA for browser-facing paths.                                                    |
| API routes / server actions           | `.agents/rules/security.md`, `supabase.md`, `error-handling.md`, `performance.md`.                                                               | `npm run qa`.                                                                                            |
| Analytics / tracking                  | `.agents/rules/analytics.md`, `security.md`.                                                                                                     | `npm run qa`; verify events only when instrumentation changed.                                           |
| Performance / interaction work        | `.agents/rules/performance.md`, `lazy-loading.md`, `clean-code.md`.                                                                              | `npm run qa`; inspect build/bundle output when bundle size is relevant.                                  |
| Refactor / cleanup                    | `.agents/rules/clean-code.md`, `file-organization.md`, plus the domain rules for touched files.                                                  | `npm run qa`.                                                                                            |
| Bug fix                               | Domain rules for the failing surface, then `clean-code.md`; add `error-handling.md` for async/catch paths.                                       | Reproduce or characterize when possible, then `npm run qa`.                                              |
| Tests                                 | `.agents/workflows/self-review.md` plus domain rules for the code under test.                                                                    | `npm run qa`; targeted test command is fine before the full loop.                                        |
| Docs / specs / plans / artifacts      | `.agents/references/artifact-layers.md`; use `.docs/templates/spec.md` only for specs and `.plans/templates/plan.md` only for plans.             | For docs-only edits run `npm run qa`; for generated artifacts, verify with the intended renderer/parser. |

Do not read spec or plan templates for unrelated code-only tasks.

## Rule catalog

| File                                 | Load when                                                                  |
| ------------------------------------ | -------------------------------------------------------------------------- |
| `.agents/rules/qa-loop.md`           | Any repo-changing task.                                                    |
| `.agents/rules/styling.md`           | Touching TSX classes, Tailwind tokens, shadcn components, or dark mode.    |
| `.agents/rules/responsiveness.md`    | Any UI layout or viewport-sensitive change.                                |
| `.agents/rules/accessibility.md`     | Any UI or interactive behavior change.                                     |
| `.agents/rules/performance.md`       | Browser/runtime performance, Server Components, Web Vitals, bundle impact. |
| `.agents/rules/lazy-loading.md`      | Heavy components, below-the-fold sections, third-party widgets.            |
| `.agents/rules/clean-code.md`        | Any code change or refactor.                                               |
| `.agents/rules/file-organization.md` | New files, moved files, module boundaries, imports.                        |
| `.agents/rules/forms.md`             | Forms, validation, inputs, submissions.                                    |
| `.agents/rules/i18n.md`              | User-facing strings or locale routing.                                     |
| `.agents/rules/security.md`          | Auth, API, env, CSP, secrets, RLS, third-party integrations.               |
| `.agents/rules/error-handling.md`    | Async failures, `catch`, logging, error boundaries.                        |
| `.agents/rules/supabase.md`          | Supabase clients, queries, migrations, generated DB types.                 |
| `.agents/rules/analytics.md`         | PostHog events, event names, client/server tracking.                       |
| `.agents/rules/admin.md`             | Admin routes, role gates, service-role access, `profiles.role`.            |

## References and workflows

References are lookup material, not always-on rules:

- `.agents/references/artifact-layers.md` - artifact taxonomy and ambiguous-task inference.
- `.agents/references/key-files.md` - important project paths.
- `.agents/references/shared-components.md` - shared UI component map.
- `.agents/references/analytics.md` - event catalog.

Workflows:

- `.agents/workflows/qa-loop.md` - step-by-step QA procedure.
- `.agents/workflows/self-review.md` - checklist before declaring done.
- `.agents/workflows/multi-agent.md` - parent/subagent contract.

## Tool-specific adapters

Keep adapters thin and repo-local. They should load or point to AGENTS.md, not restate the whole rule set.

- `CLAUDE.md`, `.claude/commands/*.md` - Claude Code memory and slash commands.
- `GEMINI.md` - Gemini reminder stub.
- `.cursor/rules/*.mdc`, `.cursor/mcp.json` - Cursor rules and MCP config.
- `.clinerules/00-base.md`, `.clineignore` - Cline rules.
- `.aider.conf.yml`, `CONVENTIONS.md` - Aider inputs and shared conventions pointer.
- `.codex/setup.sh`, `.mcp.json` - Codex / MCP setup.
- `.agents/skills/`, `skills-lock.json` - universal agent skills and lockfile.

## Completion contract

- If no files changed, report the inspected files and the verdict; do not claim QA ran.
- If files changed, check `git status`, run the required QA loop, run self-review, and report commands with exit status.
- If the QA loop reaches its cap, write the blocker plan required by `.agents/rules/qa-loop.md` and report that path.
- Commit messages use Conventional Commits. Push via `npm run push` only when a remote/upstream exists and a push/release is intended.
